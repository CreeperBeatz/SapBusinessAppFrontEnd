package com.company;

import javax.swing.*;

//actionListener for login button
//if there is content on both boxes, send a request to the server for login
    //Backend sends an User Object
//If User == trader
    //Open TraderForm with trader
//if user == administrator
    //open AdministratorForm with administrator
//else
    //Unsuccessful login attempt
public class LoginForm {
    private JTextField textField1;
    private JPasswordField passwordField1;
    private JButton loginButton;
}
